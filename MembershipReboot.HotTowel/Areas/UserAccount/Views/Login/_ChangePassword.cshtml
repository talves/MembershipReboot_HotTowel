@model MembershipReboot.HotTowel.Areas.UserAccount.Models.ChangePasswordInputModel

@using (Html.BeginForm("JsonChangePassword", "Membership", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { id = "changePasswordForm", @class = "form-horizontal" }))
{
    @Html.AntiForgeryToken()

    <fieldset>
        <legend>Change Password</legend>
        @Html.EditorForModel()
        <div class="control-group">
            <div class="controls">
                <input type="submit" value="Send Request" class="btn btn-primary" />
            </div>
        </div>
        <div class="alert alert-success" style="display:none">
            <strong>Success!</strong>&nbsp;<span></span>
        </div>
        @Html.ValidationSummary()
    </fieldset>
}