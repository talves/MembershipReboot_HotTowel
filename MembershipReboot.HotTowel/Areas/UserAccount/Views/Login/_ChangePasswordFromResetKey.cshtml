@model MembershipReboot.HotTowel.Areas.UserAccount.Models.ChangePasswordFromResetKeyInputModel

@using (Html.BeginForm("JsonChangePasswordFromResetKey", "Membership", FormMethod.Post, new { id = "changePasswordFromResetKeyForm", @class = "form-horizontal" }))
{
    @Html.AntiForgeryToken()

    <fieldset>
        <legend>Change Password from Reset Key</legend>
        @Html.EditorForModel()
        <div class="control-group">
            <div class="controls">
                <input type="submit" value="Change Password" class="btn btn-primary" />
            </div>
        </div>
        <div class="alert alert-success" style="display:none">
            <strong>Success!</strong>&nbsp;<span></span>
        </div>
        @Html.ValidationSummary()
    </fieldset>
}