@model MembershipReboot.HotTowel.Areas.UserAccount.Models.ChangeEmailFromKeyInputModel

@using (Html.BeginForm("JsonChangeEmailFromKey", "Membership", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { id = "changeEmailFromKeyForm", @class = "form-horizontal" }))
{
    @Html.AntiForgeryToken()

    <fieldset>
        <legend>Change Email from Key</legend>
        @Html.EditorForModel()
        <div class="control-group">
            <div class="controls">
                <input type="submit" value="Verify Change" class="btn btn-primary" />
            </div>
        </div>
        <div class="alert alert-success" style="display:none">
            <strong>Success!</strong>&nbsp;<span></span>
        </div>
        @Html.ValidationSummary()
    </fieldset>
}